{
  "name": "notes",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scriptsComments": {
    "dev": "make the server track changes, changes to the application's code will cause the server to restart automatically"
  },
  "author": "Thomas",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
